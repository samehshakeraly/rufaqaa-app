"""Standalone operational scripts for the Rufaqaa backend.

Top-level (sibling of ``app``) so they run as ``python -m scripts.<name>`` with
the container/working directory on ``sys.path`` — see ``scripts.seed_demo``.
"""
